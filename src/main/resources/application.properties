spring.application.name=discount-backend

# --- MySQL connection (DigitalOcean Managed Database) ---
spring.datasource.url=jdbc:mysql://db-mysql-nyc3-51512-do-user-40202141-0.a.db.ondigitalocean.com:25060/defaultdb?useSSL=true&requireSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=doadmin
spring.datasource.password=AVNS_uNCYzVxNNzlLLSy3SHh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- HikariCP: fail fast instead of silently retrying forever ---
spring.datasource.hikari.connection-timeout=5000
spring.datasource.hikari.initialization-fail-timeout=5000